# Baseline Reproducer Skill

中文 | [English](README.en.md)

一个用于审计和复现已发表 ML/AI 基线的证据门控 Agent Skill。

```text
工件审计 → checkpoint 冒烟测试 → evaluator 回放 → checkpoint 指标复现
→ 训练冒烟测试 → 完整训练复现
```

本 Skill 严格区分部署、推理、评测器、公开权重和训练流程的证据，并将证据充分的停止决定视为有效结果，而不是强行补齐缺失信息。

## 安装

将 `baseline-reproducer/` 复制或软链接到 Codex、Claude Code、Cursor 或其他兼容 Agent Skills 客户端的 Skill 目录。

```bash
mkdir -p ~/.codex/skills
ln -s "$(pwd)/baseline-reproducer" ~/.codex/skills/baseline-reproducer
```

然后可以这样调用：

> 使用 `$baseline-reproducer` 审计这篇论文的官方工件，并复现其公开材料能够支持的最强主张。

> <strong>运行时说明：</strong>Skill 被调用时会自动加载 `SKILL.md`，但不会自动读取仓库根目录的 README。为确保这里定义的评级规则实际生效，项目会把英文 README 中的评级内容同步到 Skill 内的 `references/grading-rubric.md`，并由 `SKILL.md` 强制要求在评级前读取。

## 主要原则

- 优先运行官方代码、公开权重和官方评测器，而不是从论文重新生成实现；
- 将论文拆成原子主张，分别记录证据等级；
- 区分 [evaluator replay](#evaluator-replay)、checkpoint 指标复现和完整训练复现；
- 固定代码 revision、权重 hash、数据 split、seed 和评测协议；
- 保留失败运行、兼容性修改、资源干扰和被作废的结果；
- 缺少关键数据、权重或协议时明确停止，不做未经标注的替代；
- 在公开权重验证通过且训练 Gate 完整后，才启动昂贵训练。



## 论文指标的链路完整度评级

评级对象不是整篇论文，而是论文表格中的一个<strong>原子指标主张</strong>。例如：


| 模型    | 数据集 1 | 数据集 2 |
| ----- | ----- | ----- |
| SOTA1 | a1    | a2    |
| Ours  | b1    | b2    |


这里共有四个主张：

- `b1`、`b2` 是论文对自身模型作出的两个<strong>本模型主张</strong>；
- `a1`、`a2` 是论文引用或重新测量的两个<strong>其他模型主张</strong>。

一篇论文可能包含许多实验表格。通常先选择最重要的若干表格，例如主结果、关键消融和核心泛化实验，再将其中每个关键单元格登记成独立主张。不要用一个总评级覆盖整篇论文。

消融实验按复现成本排序：若需要增删模型模块、重新训练或重新发布权重，通常优先级低于主结果；若只需改变测试时设置、推理参数、输入条件或 evaluator 开关，且可以复用同一公开权重，则属于低成本、可控的消融，可优先验证。任何测试时变更仍须单独冻结协议，不能与主结果混成同一条指标链路。

### 两类、四个独立评级

每个主张分别记录：

1. <strong>交付完整度</strong>：作者公开了什么。
  - <strong>TD（Test Delivery Completeness，测试交付完整度）</strong>：作者是否交付了验证该数值所需的权重、测试代码、测试数据及划分和评测协议。
  - <strong>RD（Training Delivery Completeness，训练交付完整度）</strong>：作者是否交付了产生该权重所需的训练代码、训练数据及划分、训练设置和依赖权重。
2. <strong>复现完整度</strong>：复现者实际验证到哪里。
  - <strong>TR（Test Reproduction Completeness，测试复现完整度）</strong>：复现者是否实际运行并审计了权重到指标的测试链路。
  - <strong>RR（Training Reproduction Completeness，训练复现完整度）</strong>：复现者是否实际运行并审计了数据到训练权重再到指标的完整链路。

交付评级描述“作者公开了什么”；复现评级描述“复现者实际验证到哪里”。四个评级不能互相替代。

<strong>交付对复现的约束：</strong>复现完整度受交付完整度约束；如果目标链路所需的关键材料没有被完整交付，复现通常也无法达到完整等级。评估复现完整度时，复现者可以主动从网络寻找论文明确使用且可公开获取的训练集及其精确划分，也可以按照论文或官方仓库已经说明的规则运行训练数据预处理；这些工作只是获取或确定性重建已明确的材料，不降低复现评分。但是，复现者没有必要自行补全作者未交付的训练代码，也不应猜测论文或官方仓库未给出的训练参数、停止条件或 checkpoint 选择规则；遇到这些情况时，应直接记录相应条件缺失，并据此确定复现等级。复现者自行编写训练实现或猜测配置所得的结果，如有报告价值，应标记为独立实现或独立协议结果，不能视为目标主张的严格复现。

### 等级定义

四个指标分别评级，不能用一张通用表互相推导。基础等级从低到高为 `F`、`I`、`M`、`E`、`O`；其中 `M+` 和 `E+` 用于标记已明显超过本级基本要求、但尚未达到下一基础等级的情况。标签沿用“Failed / Improving / Medium / Excellent / Outstanding”的分级命名[[1](https://www.zhihu.com/question/495085246), [2](https://www.dingteam.com/article/285)]，这里分别衡量<strong>公开材料为主张提供的可核查基础</strong>和<strong>实际复现产生的可核查证据</strong>。其中，交付完整度只根据作者公开了什么来评级，与复现者是否运行、是否成功运行以及最终数值是否匹配无关；复现完整度才根据实际执行和审计到哪里来评级。评级不评价数值本身的高低，也不延伸为对整篇论文总体可信度的评级。`NR`（Not Rated / Not Applicable）表示尚未评级或不适用，不属于上述等级。

以下定义均按单个已冻结主张判定。例如，`Paper X` 表 1 中“模型在 `Dataset 1` 上得到 `b1=82.7`”是一条主张；同一论文中的其他单元格应分别评级。

#### TD：测试交付完整度

只根据作者公开材料的范围、明确程度和主张对应关系评级，不考虑复现者是否实际运行、运行是否成功或所得数值是否匹配。按材料所达到的<strong>最高等级</strong>评级，并采用“关键短板决定等级”的原则：不能用若干完整项目抵消 checkpoint 身份、测试集划分或评测规则等关键缺口。


| 等级    | 定义                                                                                                                                                                                                                                                                            | 示例（`Paper X / b1=82.7`）                                                                                                                                                                                                                                                                      |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <strong>O</strong> | 1. 测试运行环境与版本号； 2. 与目标主张准确对应的模型权重； 3. 论文使用的测试集及其精确划分； 4. 全部测试参数； 5. 测试规则（数据预处理、推理、评测、聚合及随机性处理等）清晰完整，无须猜测或替换任何影响结论的设置。                                                                                                                                                          | 作者提供 `environment.yml`，其中指定 Python 3.10.13、PyTorch 2.1.2 和 CUDA 12.1；表 1 明确对应 `paper-x-step-50000.ckpt`，并给出 SHA-256；`test_ids.txt` 列出用于评测的 10,000 个样本 ID。测试命令指定 `seed=42`、`batch_size=64`，图像先缩放到 256 像素再做 224×224 中心裁剪；随仓库发布的 `accuracy.py` 明确规定对全部样本计算 top-1 accuracy，并将该计算对应到论文所报的 `82.7`。 |
| <strong>E</strong> | 1. 与目标主张对应的 checkpoint、测试集及划分、测试参数和测试规则均已交付； 2. 所有影响结果含义的设置均明确； 3. 仅缺少少量不改变测试协议的操作性信息，例如启动命令示例或非关键依赖的精确版本。 <strong>可评为 E+：</strong> 1. 上述材料均可直接定位，且版本和主张对应关系明确； 2. 只缺一项非关键操作性信息； 3. 不需要复现者补充任何影响结果的设置。                                                                                     | <strong>E：</strong>作者提供 `paper-x-step-50000.ckpt`、10,000 个样本 ID、`seed=42`、224×224 中心裁剪规则和 `accuracy.py`，但没有提供完整启动命令，也未锁定日志工具等非关键依赖的版本。 <strong>E+：</strong>上述材料均有明确版本并直接对应表 1，只缺使用的 GPU 具体型号；该信息不改变已公开的测试协议。                                                                                                           |
| <strong>M</strong> | 1. 已交付 checkpoint、测试数据、推理代码或 evaluator 中的主要部分，但至少一项关键交付不完整； 2. checkpoint 版本、测试集精确划分、关键推理参数、evaluator 或聚合规则中存在缺失或歧义； 3. 现有材料只能定义部分测试链路，或者只能定义一条不能与论文原协议严格对应的完整链路。 <strong>可评为 M+：</strong> 1. checkpoint 到指标的全部环节均有对应材料； 2. 模型权重、测试集和评测方法基本对应目标主张； 3. 只缺原始 seed、精确依赖版本、次要测试参数或统计细节等一项有限信息。 | <strong>M：</strong>作者只发布 `table1_predictions.json` 和 `accuracy.py`，没有发布生成这些预测的 checkpoint；或者仓库同时包含 `test-v1.txt` 和 `test-v2.txt`，但论文没有说明表 1 使用哪一份。 <strong>M+：</strong>作者发布 `paper-x-step-50000.ckpt`、`test_ids.txt` 和 `accuracy.py`，并指定 224×224 中心裁剪和 `batch_size=64`，但没有公开表 1 使用的 seed。                              |
| <strong>I</strong> | 1. 只能找到论文数值、仓库、权重链接、数据入口或零散代码； 2. 关键工件严重缺失，或现有工件彼此无法对应； 3. 尚不能组成可执行的主张级测试链路。                                                                                                                                                                                                  | 仓库只提供名为 `model_final.ckpt` 的权重链接，没有配置文件或表格映射说明它是否对应表 1；`Dataset 1` 官网有多个测试划分，仓库既未指定其中一个，也没有提供评测命令。                                                                                                                                                                                           |
| <strong>F</strong> | 1. 已确认公开工件与目标主张冲突，例如权重属于另一模型变体、测试集使用另一划分，或评测协议被替换； 2. 这些材料即使能够运行，所得结果也不能归属于目标主张。                                                                                                                                                                                              | 下载页把 `paper-x-large.ckpt` 标为表 1 权重，但其配置显示 `hidden_size=768`，而表 1 的 Large 模型应为 `hidden_size=1024`；因此该权重的测试结果不能归属于 `b1=82.7`。                                                                                                                                                                  |




#### RD：训练交付完整度

只根据作者公开材料的范围、明确程度和主张对应关系评级，不考虑复现者是否实际训练、是否产出 checkpoint、测试结果是否匹配论文。按材料所达到的<strong>最高等级</strong>评级，并采用“关键短板决定等级”的原则：不能用完整的代码或环境配置抵消训练数据、依赖权重、关键训练参数等核心缺口。


| 等级    | 定义                                                                                                                                                                                                                                                             | 示例（`Paper X / b1=82.7`）                                                                                                                                                                                                                                                                                                                      |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <strong>O</strong> | 1. 训练运行环境与版本号； 2. 训练数据与精确划分； 3. 依赖权重与初始化材料； 4. 全部训练参数、停止条件和 checkpoint 选择规则； 5. 训练规则（数据预处理、采样、优化、分布式训练及随机性处理等）清晰完整，无须猜测或替换任何影响结论的设置。                                                                                                                           | 作者提供 `environment.yml`，指定 Python 3.10.13、PyTorch 2.1.2 和 CUDA 12.1；`train_ids.txt` 与 `val_ids.txt` 分别列出 1,200,000 个训练样本和 50,000 个验证样本；训练从 `base-vit-b16.ckpt`（附 SHA-256）开始。`train.yaml` 指定 `batch_size=4096`、`lr=1e-3`、AdamW、cosine 调度、`seed=42`、训练 300 epochs，并规定按验证集 top-1 accuracy 选择最佳 checkpoint；`train.py` 和文档明确给出图像增强、采样及 8 卡分布式训练规则。 |
| <strong>E</strong> | 1. 训练数据及划分、模型配置、依赖权重、训练参数和训练规则均已交付并能与目标主张对应； 2. 所有影响训练含义和 checkpoint 选择的设置均明确； 3. 仅缺少少量不改变训练协议的操作性信息，例如启动命令示例或非关键依赖的精确版本。 <strong>可评为 E+：</strong> 1. 上述材料均可直接定位，且版本和主张对应关系明确； 2. 只缺一项非关键操作性信息； 3. 不需要复现者补充任何影响训练结果含义的设置。                                                   | <strong>E：</strong>作者提供 `train.py`、`train.yaml`、`base-vit-b16.ckpt` 和完整样本 ID，但 `train_ids.txt` 需要运行公开的 `make_split.py --version 2.1` 确定性生成。 <strong>E+：</strong>数据划分、基模型哈希、全部超参数、停止条件和 checkpoint 选择规则均直接提供并明确对应表 1，只缺 8 张 GPU 的具体型号。                                                                                                                                  |
| <strong>M</strong> | 1. 已交付训练数据、训练代码、模型配置或依赖权重中的主要部分，但至少一项关键交付不完整； 2. 数据精确划分、初始化材料、关键超参数、停止条件或 checkpoint 选择规则中存在缺失或歧义； 3. 现有材料只能定义部分训练链路，或者只能定义一条不能与论文原训练协议严格对应的完整链路。 <strong>可评为 M+：</strong> 1. 数据到 checkpoint 的全部环节均有对应材料； 2. 模型、主要训练数据和训练方法基本对应目标主张； 3. 只缺原始 seed、精确依赖版本、次要超参数或统计细节等少量有限信息。 | <strong>M：</strong>作者公开训练代码和 `Dataset 1` 下载地址，但没有发布训练样本划分，也未说明初始化使用 `base-vit-b16.ckpt` 还是随机权重。 <strong>M+：</strong>作者提供训练集 ID、基模型、`train.py` 和主要超参数，数据到 checkpoint 的各环节均有材料，但没有公开 `seed` 和 warmup 步数。                                                                                                                                                                 |
| <strong>I</strong> | 1. 只能找到论文描述、代码仓库、数据入口或零散训练代码； 2. 训练数据、精确划分、关键依赖权重、模型配置或训练入口严重缺失，或者现有工件彼此无法对应； 3. 尚不能组成可执行的目标训练链路。                                                                                                                                                              | 仓库只有模型结构和数据加载器；没有 `Dataset 1` 的训练划分、训练入口、优化器配置或基模型链接，无法根据公开材料组成目标训练链路。                                                                                                                                                                                                                                                                       |
| <strong>F</strong> | 1. 已确认训练工件与目标主张冲突，例如代码或配置属于另一模型、训练数据使用另一版本，或训练协议被替换； 2. 这些材料即使能够运行，所得 checkpoint 也不能归属于目标主张。                                                                                                                                                                   | 仓库把 `train_large.yaml` 标为表 1 Large 模型配置，但其中 `hidden_size=768`，而论文定义为 `1024`；该配置对应另一模型，不能用于交付表 1 目标 checkpoint 的训练链路。                                                                                                                                                                                                                         |




#### TR：测试复现完整度

根据实际完成的测试和保留的证据评级。`TR` 同时考察测试协议的对齐程度、测试链路的执行范围以及复现数值与论文声明的一致程度；数值接近不能弥补权重、数据或协议未对齐。因本地条件使用不同但兼容的 CUDA、驱动或硬件版本，只要已记录且没有改变计算语义、精度模式或指标结果，不构成减分项。


| 等级    | 定义                                                                                                                                                                                                                                                                                                                                                    | 示例（`Paper X / b1=82.7`）                                                                                                                                                                                                                                                               |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <strong>O</strong> | 1. 使用论文或官方仓库指定的测试运行环境及版本配置，或经验证不改变计算语义、精度模式和指标结果的兼容环境； 2. 使用与目标主张准确对应的模型权重； 3. 在论文指定的测试集及其精确划分上运行； 4. 严格采用论文或官方仓库指定的运行方式、参数配置和指标计算方式； 5. 完成端到端全量测试后，得到与论文声明一致的指标数值，误差不超过预先规定的极小容差； 6. 保留实际命令、运行日志、逐样本结果和最终指标，使运行过程与数值结论均可核查。                                                                                                                          | 在作者提供的 Python 3.10.13、PyTorch 2.1.2、CUDA 12.1 环境中，加载经 SHA-256 核验的 `paper-x-step-50000.ckpt`，对 `test_ids.txt` 中全部 10,000 个样本按 `seed=42`、`batch_size=64` 和 224×224 中心裁剪运行 `accuracy.py`，得到 top-1 accuracy `82.69`；论文声明为 `82.7`，按保留一位小数的规则两者一致。命令、日志和逐样本预测均已保存。                          |
| <strong>E</strong> | 1. 使用与目标主张对应的模型权重和论文指定的完整测试集； 2. 完成端到端全量测试，并按论文的指标计算方式得到有效结果； 3. 参数配置、重复次数或测试规则存在少量已记录的不完整或差异，但不改变测试任务和指标含义； 4. 复现数值与论文声明接近，或差异能够结合已记录的协议差异进行合理解释； 5. 保留命令、日志、逐样本结果和协议差异记录。 <strong>可评为 E+：</strong> 1. 权重、测试集、运行方式、参数和指标计算均与论文严格对齐，运行环境为指定环境或经验证等价的兼容环境； 2. 已完成端到端全量测试并保留完整证据； 3. 复现数值与论文声明存在超过 O 级极小容差的差异，因而不能评为 O，或仅缺少一项不影响结果解释的操作性证据。                          | <strong>E：</strong>使用表 1 对应 checkpoint 和全部 10,000 个测试样本，按作者指定的预处理和 `accuracy.py` 完成全量评测；论文结果是 `seed=40–44` 五次运行的均值，但受算力限制只完成 `seed=42`，得到 `82.6`，与论文的 `82.7` 接近。 <strong>E+：</strong>环境、checkpoint、样本列表、`seed=42`、预处理和 `accuracy.py` 均与作者设置严格一致，完整运行得到 `82.4`；该差异超过预先规定的 `±0.1` 容差，因此不能评为 O，但命令、日志和逐样本结果均已完整保存。 |
| <strong>M</strong> | 1. 已完成有效的部分测试链路，例如单样本或固定子集推理、[evaluator replay](#evaluator-replay)，或者使用替代协议完成全量测试； 2. 模型权重、测试集划分、关键参数或指标计算方式至少有一项不能与论文严格对应； 3. 已得到可核查的局部结果或独立协议结果，但该结果不能直接作为论文声明数值的复现； 4. 已记录实际使用的材料、命令和主要协议差异。 <strong>可评为 M+：</strong> 1. 使用与目标主张对应的模型权重，在完整测试集上完成全量测试； 2. 主要运行方式和指标计算与论文基本一致； 3. 仅因作者未公开 seed、精确依赖版本、次要参数或统计细节等有限信息，必须采用预先固定的独立设置； 4. 已保留完整结果和差异记录，但无法进行严格数值对齐。 | <strong>M：</strong>没有可用 checkpoint，只能把作者发布的 `table1_predictions.json` 输入 `accuracy.py`，重算出 `82.7`；这只复现了评分环节。 <strong>M+：</strong>使用表 1 对应 checkpoint 和全部 10,000 个样本完成评测，但作者未公开 seed；复现者预先固定 `seed=42`，得到 `82.5`，并完整记录这一协议差异。                                                                                    |
| <strong>I</strong> | 1. 已开始实际测试，并至少完成环境启动、代码导入、模型加载、数据读取或 evaluator 接口检查中的一项； 2. 测试链路被缺失工件、配置错误、依赖问题或资源问题阻塞； 3. 尚未得到能够判断目标主张的有效指标结果； 4. 已记录失败位置、错误日志和当前阻塞项。                                                                                                                                                                                                                | 已创建 conda 环境并成功加载 `paper-x-step-50000.ckpt`，但读取测试数据时发现缺少 `test_ids.txt`，无法确定论文使用的 10,000 个样本，因此尚未计算指标；错误日志和缺失项已记录。                                                                                                                                                                    |
| <strong>F</strong> | 1. 已完成的运行被确认使用了错误的模型权重、测试集划分、关键参数或指标计算方式，或者结果存在无法修复的完整性问题； 2. 该次运行已经作废，所得数值不能归属于目标主张； 3. 作废原因和相关证据已明确记录。                                                                                                                                                                                                                                              | 完成评测后核对配置，发现实际加载的 `paper-x-base.ckpt` 为 `hidden_size=768`，而表 1 的 Large 模型应为 `hidden_size=1024`；得到的 `79.3` 已作废，并记录了权重路径、配置和作废原因。                                                                                                                                                       |


<strong>TR 数值一致性与 O 级判定：</strong>应在运行前根据论文报告精度、指标波动性和领域惯例声明绝对或相对容差；复现值与论文值之差不超过该容差时视为一致，论文只报告有限小数位时，复现值按相同精度取整后相同也可视为一致。例如，论文报告 `82.7` 且预先规定绝对容差为 `±0.1`，则 `82.69` 与 `82.7` 一致，而 `82.4` 的绝对误差为 `0.3`，超过容差，视为不一致。容差不得在看到结果后为获得一致结论而放宽。`TR-O` 同时要求测试协议严格对齐和数值一致；若完整、严格的测试结果超过预先规定的容差，则不能评为 `TR-O`，应根据链路执行与证据完整程度评为其他等级，并将数值结论另行记为 `not_matched` 或 `contradicted_under_pinned_protocol`；反之，数值落入容差也不能弥补权重、数据或协议未对齐。

#### RR：训练复现完整度

<strong>算力准入条件：</strong>开始评级前，应比较本机可用算力资源与论文或官方仓库声明的训练算力资源。如果本机可用算力资源低于论文或官方仓库的训练要求，则跳过训练复现，不对 `RR` 进行等级判定，并记为 `NR`。不得通过缩小模型、减少训练数据、缩短训练轮次或改变精度配置来降低算力需求后继续沿用原主张的 `RR` 评级；如需进行此类实验，应将其登记为独立协议结果，而不是目标主张的训练复现。


| 等级    | 定义                                                                                                                                                                                                                                                                                                                                                                                     | 示例（`Paper X / b1=82.7`）                                                                                                                                                                                                                                                                 |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <strong>O</strong> | 1. 使用论文或官方仓库指定的训练运行环境及版本配置，或经验证不改变训练语义、精度模式和结果解释的兼容环境； 2. 使用与论文相同的模型初始设置，包括从头训练（from scratch）或从论文指定的基模型、预训练权重和初始化配置开始； 3. 使用论文指定的训练集及其精确划分； 4. 严格采用论文或官方仓库声明的训练算法、训练方式和全部配置，包括数据处理、采样、优化、学习率调度、分布式训练、随机性处理、停止条件和 checkpoint 选择规则； 5. 完成端到端训练并产出新的模型权重； 6. 按论文指定的测试协议评测该新训练模型，得到与论文声明一致的指标数值，误差不超过预先规定的极小容差； 7. 保留训练与测试命令、日志、数据和依赖权重身份、关键 checkpoint、逐样本测试结果及最终指标，使从模型初始化到论文指标的完整链路可核查。 | 在作者指定的 Python 3.10.13、PyTorch 2.1.2 环境中，从附 SHA-256 的 `base-vit-b16.ckpt` 初始化，使用 `train_ids.txt` 中 1,200,000 个样本，按 `train.yaml` 在 8 张 GPU 上训练 300 epochs，并按验证集 top-1 accuracy 选择新生成的 `epoch-287.ckpt`。随后按表 1 测试协议评测，得到 `82.68`；论文声明为 `82.7`，按一位小数报告时一致。训练与测试命令、日志、checkpoint 和逐样本结果均已保存。 |
| <strong>E</strong> | 已冻结训练数据及划分、模型配置、训练参数、依赖权重、停止条件和 checkpoint 选择规则，完成端到端训练、全量测试及训练轨迹审计；仅有少量不改变训练含义的可追溯兼容处理。 <strong>可评为 E+：</strong>在此基础上完成预先规定的多次独立训练或不确定性分析，并复核数据、checkpoint 和聚合身份；仅未达到干净环境完整重建或第三方直接复核所需的完整证据水平。                                                                                                                                                                                                    | <strong>E：</strong>使用相同基模型、完整训练集、算法和主要超参数完成训练与全量评测；论文要求 `seed=40–44` 五次训练均值，但受算力限制只完成 `seed=42`，新模型得到 `82.6`。 <strong>E+：</strong>初始化、数据、训练配置、checkpoint 选择和测试协议均严格对齐，完整训练得到 `82.3`，超过预先规定的 `±0.1` 容差；链路与证据完整，但数值未达到 O。                                                                                           |
| <strong>M</strong> | 已执行部分有效训练链路，或已完成一次训练与评测但关键条件无法与论文严格对齐；例如只完成短步数训练或训练子集 smoke test，或者数据、seed、规模、超参数或停止规则存在重要差异。 <strong>可评为 M+：</strong>已按基本对齐的协议完成一次端到端训练与全量测试，只缺少量对严格对齐有影响的信息，或尚未完成重复训练/不确定性分析。                                                                                                                                                                                                                     | <strong>M：</strong>仅使用训练集前 10,000 个样本训练 5 epochs，确认损失下降并生成可加载 checkpoint；该结果不能代表论文的完整训练。 <strong>M+：</strong>使用完整训练集和相同基模型完成 300 epochs 训练与全量测试，但作者未公开学习率 warmup 步数；复现者预先固定为 10 epochs，新模型得到 `81.9`，并记录协议差异。                                                                                                      |
| <strong>I</strong> | 已完成材料审计或尝试训练，但仍被关键数据、配置、依赖或资源问题阻塞，尚未产生可用于完整评测的有效 checkpoint。                                                                                                                                                                                                                                                                                                                           | 环境和数据加载均已跑通，但下载的 `base-vit-b16.ckpt` 校验失败，训练在初始化阶段停止，尚未产生可评测的新 checkpoint；错误日志和阻塞项已保存。                                                                                                                                                                                                  |
| <strong>F</strong> | 已确认实际训练使用了错误数据、模型配置或训练协议；所得 checkpoint 与结果已经作废，不能归属于目标主张。                                                                                                                                                                                                                                                                                                                              | 训练和评测完成后发现实际使用的是 `train-v1.txt`，而目标主张明确使用 `train-v2.txt`；新模型得到的 `80.1` 不能归属于表 1 的 `b1`，该次训练已作废并记录原因。                                                                                                                                                                                    |




### 示例评级

假设对上述主结果表审计后得到：


| 主张   | 类型   | TD  | RD  | TR  | RR  | 数值结论                    |
| ---- | ---- | --- | --- | --- | --- | ----------------------- |
| `b1` | 本模型  | E   | M+  | E+  | M   | matched                 |
| `b2` | 本模型  | M   | I   | M   | NR  | not_strictly_comparable |
| `a1` | 其他模型 | M   | NR  | M   | NR  | author_table_only       |
| `a2` | 其他模型 | F   | NR  | F   | NR  | invalidated             |


- `b1`<strong>：</strong> 精确权重、测试集和 evaluator 均公开并已复现；训练数据需按公开脚本重建，但尚只完成训练子集。
- `b2`<strong>：</strong> 权重公开，但作者未给 seed 和完整测试划分；训练数据未公开。只能报告固定独立协议结果。
- `a1`<strong>：</strong> 本文没有重新运行 SOTA1，只引用其原论文数值；可回到 SOTA1 原论文继续单独评级。
- `a2`<strong>：</strong> 论文使用的 SOTA1 数据划分与本表 Ours 不同，却放在同一列直接比较，该比较链路无效。

对于其他模型主张，要进一步记录数值来源：`cited_from_original_paper`、`rerun_by_current_authors` 或 `unclear`。只有当前论文确实使用同一协议重新运行了对方模型，`a1/a2` 才能直接继承当前表格的测试链路；单纯转抄原论文数字不能视为本文完成了复现。


## 专有名词解释

<a id="evaluator-replay"></a>
<strong>Evaluator replay（评测器回放）</strong>就是用作者公开的输出和评测代码，重新计算论文中的分数。例如，作者公开了 1,000 个生成视频和 VBench 代码；我们重新评分得到 `81.0`，论文也报告 `81.0`。这只能说明评分流程可以复现，不能说明模型生成或训练过程可以复现。


<a id="api-call"></a>
<strong>修改 API 调用</strong>是为了适配新版依赖，只更换已经失效的函数名、参数名或调用写法。例如把旧参数 `torch_device="cuda"` 改成新版参数 `device="cuda"`；传入值和实际计算不变。


<a id="independent-seed"></a>
<strong>复现者自行选用的 seed</strong>是指作者未公开原实验的随机种子时，复现者自己选择一组种子，例如 `42、43、44、45、46`。这些种子应在运行前固定并记录，避免看到结果后只挑表现好的种子。由于它们不是作者原来的种子，不能称为精确恢复作者实验。


<a id="protocol"></a>
<strong>协议（protocol）</strong>是产生并计算这个指标时使用的一整套固定设置，例如测试集划分、输入预处理、推理步数、seed、样本数和评分方法。只把旧 API 参数名改成新名称不算改变协议；改用另一份测试集或不同推理步数则算。

## 许可证

MIT